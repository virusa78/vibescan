import { createAction } from '../../middleware/operations.js'
import updateNotificationSettings from '../../actions/updateNotificationSettings.js'

export default createAction(updateNotificationSettings)
