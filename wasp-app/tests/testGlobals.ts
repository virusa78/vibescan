import {
  afterAll,
  afterEach,
  beforeAll,
  beforeEach,
  describe,
  expect,
  it,
  test,
  jest,
} from '@jest/globals';

export { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it, test, jest };
export const vi = jest;
