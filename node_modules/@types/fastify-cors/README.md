This is a stub types definition for @types/fastify-cors (https://github.com/fastify/fastify-cors#readme).

fastify-cors provides its own type definitions, so you don't need @types/fastify-cors installed!